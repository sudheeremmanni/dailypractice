package abstractdemo;

public abstract class BookMyShowService {
      /*  public Ticket createTicket(int seats, int classType, int row){

        }
        public Ticket bookTicket(int seats, int classType, int row){
            int addOnPrice = addAddOns(null);
            String response = payment();
            if (response.equals("Success")){
                Ticket = createTicket(seats,classType,row);
            }
        }
        public int AddOns(AddOns addOns){

        }
        protected abstract String payment();
*/
}
